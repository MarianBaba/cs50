#include <stdio.h>

int main(int argc, string argv[])
{
    int numbers[] = {20, 500, 10, 5, 100, 1, 50};
}

void linear_search(int len, int numbers[], int target)
{
    int index = -1;
    for (int i = 0; i < len; i++)
    {
        if (numbers[i] == target)
        {
            index = i;
        }
    }
    return index;
}

void binary_search(int len, int numbers[], int target)
{
    if (len % 2 == 0)
    {
        
    }
}